# PersonaPlex minimal runtime
# NOTE: PyTorch wheels are published for specific CUDA toolkits (e.g. cu121/cu124).
# Your CUDA 12.8 driver is backward-compatible with these wheels.
--extra-index-url https://download.pytorch.org/whl/cu124

torch
torchvision
torchaudio

numpy>=1.24
sounddevice>=0.4.6
soundfile>=0.12.1
huggingface-hub>=0.24.0

# PersonaPlex runtime (moshi)
# Install from NVIDIA repo (includes moshi package)
git+https://github.com/NVIDIA/personaplex.git#subdirectory=moshi
